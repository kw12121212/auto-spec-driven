{
  "name": "spec-driven",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "test": "bash test/run.sh",
    "install-skills": "bash install.sh",
    "install-skills:project": "bash install.sh --project",
    "uninstall-skills": "bash install.sh --uninstall",
    "uninstall-skills:project": "bash install.sh --uninstall --project"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "@types/node": "^20.0.0"
  }
}
